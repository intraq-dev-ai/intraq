export interface HomeDashboard {
  id: string;
  name: string;
  updatedAt?: string;
  createdAt?: string;
}

export interface HomeConversation {
  id: string;
  title: string;
  updatedAt?: string;
  createdAt?: string;
}
